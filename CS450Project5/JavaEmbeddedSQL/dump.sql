/*
	Filename: dump.sql
	Author: Walter Hiranpat
	Description: Contain a script for a complete listing 
		of your database
*/


DESCRIBE STAFF;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'STAFF';
SELECT * FROM STAFF;

DESCRIBE EXPERT;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'EXPERT';
SELECT * FROM EXPERT;

DESCRIBE SPECIALTY;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'SPECIALTY';
SELECT * FROM SPECIALTY;

DESCRIBE DIAGNOSE;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'DIAGNOSE';
SELECT * FROM DIAGNOSE;

DESCRIBE PATIENT;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'PATIENT';
SELECT * FROM PATIENT;

DESCRIBE VISIT;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'VISIT';
SELECT * FROM VISIT;

DESCRIBE VDIAGNOSES;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'VDIAGNOSES';
SELECT * FROM VDIAGNOSES;

DESCRIBE SCHEDULE;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'SCHEDULE';
SELECT * FROM SCHEDULE;

DESCRIBE TIMESLOT;
SELECT X.CONSTRAINT_NAME, X.CONSTRAINT_TYPE, X.STATUS, X.VALIDATED,
		X.SEARCH_CONDITION, Y.COLUMN_NAME, Y.POSITION
FROM USER_CONSTRAINTS X, USER_CONS_COLUMNS Y
WHERE X.CONSTRAINT_NAME = Y.CONSTRAINT_NAME
AND X.TABLE_NAME = 'TIMESLOT';
SELECT * FROM TIMESLOT;